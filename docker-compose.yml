version: '3'

services:
  loadbalancer:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app1
      - app2

  app1:
    build: .
    ports:
      - "8001:80"

  app2:
    build: .
    ports:
      - "8002:80"
